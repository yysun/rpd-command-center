import type { StoryMap } from '../types/model'

/**
 * Parse a markdown string into a StoryMap.
 * Full implementation is covered in Phase 1 (parser module).
 */
export function parse(_markdown: string): StoryMap {
  return { title: 'User Story Map', activities: [] }
}
