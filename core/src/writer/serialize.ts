import type { StoryMap, FormatMode } from '../types/model'

/**
 * Serialize a StoryMap back to markdown.
 * Full implementation is covered in Phase 1 (writer module).
 */
export function serialize(_map: StoryMap, _mode: FormatMode): string {
  return ''
}
